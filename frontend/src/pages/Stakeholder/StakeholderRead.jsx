import Stakeholder from './index';

// This file exists so AppRouter can lazy-import the /stakeholder/read/:id route.
// The actual Read UI is the side panel inside the main Stakeholder list page.
export default Stakeholder;
