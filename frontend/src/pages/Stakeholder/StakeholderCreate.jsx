import Stakeholder from './index';

// This file exists so AppRouter can lazy-import the /stakeholder/create route.
// The actual Create UI is a side panel inside the main Stakeholder list page.
export default Stakeholder;
