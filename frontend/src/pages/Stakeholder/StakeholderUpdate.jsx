import Stakeholder from './index';

// This file exists so AppRouter can lazy-import the /stakeholder/update/:id route.
// The actual Update UI is the side panel inside the main Stakeholder list page.
export default Stakeholder;
