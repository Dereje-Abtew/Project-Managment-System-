import ServiceProvider from './index';

// This file exists so AppRouter can lazy-import the /serviceprovider/update/:id route.
// The actual Update UI is the side panel inside the main ServiceProvider list page.
export default ServiceProvider;
