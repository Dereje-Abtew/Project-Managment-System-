import ServiceProvider from './index';

// This file exists so AppRouter can lazy-import the /serviceprovider/create route.
// The actual Create UI is a side panel inside the main ServiceProvider list page.
export default ServiceProvider;
